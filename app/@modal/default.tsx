// Ця частина коду взята з конспекту — "Перехоплення маршрутів"
// default.tsx обов'язковий для паралельного маршруту @modal
// Коли модалка не потрібна — нічого не рендеримо
export default function ModalDefault() {
  return null;
}
